services:
  - type: web
    name: zynochat-app
    env: node
    plan: free
    buildCommand: npm install && npm run build:next
    startCommand: npm start
    envVars:
      - key: GEMINI_API_KEY
        sync: false
